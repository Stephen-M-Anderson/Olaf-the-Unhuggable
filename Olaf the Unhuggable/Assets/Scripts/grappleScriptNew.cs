/* Friendship has ended with GrappleScriptOld, now grappleScriptNew is my best friend. */

using System;
using System.Diagnostics;
using Unity.Collections;
using UnityEngine;


public class grappleScriptNew : MonoBehaviour
{

    /* public GameObject ropeShooter;

    private SpringJoint rope;
    public int maxRopeFrameCount;
    private int ropeFrameCount;
    public LayerMask whatIsGrappleable;
    private float maxDistance = 100f;

    public LineRenderer lineRenderer;

    private void Update()
    {
        if (Input.GetButtonDown("Fire1"))
        {
            Fire();
        }
    }

    void Fire()
    {
        Vector3 mousePosition = Camera.main.ScreenToWorldPoint(Input.mousePosition);
        Vector3 position = ropeShooter.transform.position;
        Vector3 direction = mousePosition - position;

        RaycastHit hit;
        if (Physics.Raycast(position, direction, out hit, maxDistance, whatIsGrappleable))
        {
            SpringJoint newRope = ropeShooter.AddComponent<SpringJoint>();
            newRope.enableCollision = false;
            newRope.autoConfigureConnectedAnchor = false;
            newRope.connectedAnchor = ;


        }

    } */

}
